import { requireAdmin, ok } from '../../_lib/auth.js';
export async function onRequestPost(context){ const blocked=requireAdmin(context); if(blocked)return blocked; await context.env.DB.prepare(`CREATE TABLE IF NOT EXISTS products (id INTEGER PRIMARY KEY AUTOINCREMENT,slug TEXT UNIQUE NOT NULL,name TEXT NOT NULL,category TEXT,price TEXT,image TEXT,short_description TEXT,ingredients TEXT,directions TEXT,qr_education TEXT,stripe_url TEXT,featured INTEGER DEFAULT 0,visible INTEGER DEFAULT 1,created_at TEXT DEFAULT CURRENT_TIMESTAMP,updated_at TEXT DEFAULT CURRENT_TIMESTAMP)`).run(); return ok({success:true,message:'Database table created. Add products from admin.'}); }
